# Post 1 — Twitter / X (launch day)

> We benchmarked AgentLoop vs raw Claude Code on SWE-bench-Verified.
> Result: +16% resolve rate (64% → 80%).
>
> The trick isn't a better model — it's refusing to accept "done" until a
> held-out oracle proves the work is correct. The agent can't overfit to tests
> it never sees.
>
> Free & MIT. Bring your own harness. → agentloop.dev / GitHub

Alt / thread draft:
1. Agents stop halfway and claim victory. You loop them. They overfit your
   test cases. You ship broken code. Sound familiar?
2. The fix is a held-out oracle: grade the agent on inputs it has never seen,
   sealed so it can't tamper. +16% resolve rate on SWE-bench-Verified.
3. AgentLoop does this for OpenCode / Claude Code / Aider / Codex / Goose.
   Free. MIT. → github.com/instax-dutta/agentloop
